pipeline{
    agent any
    stages{
        stage ('initialize') {
            steps{
                echo 'PATH = ${PATH}'
                echo 'executed 1st command'
                echo 'M2_home = ${M2_HOME}'
                echo 'executed 2nd command' 
            }
        }
    }
}
